apiVersion: v1
kind: Service
metadata:
  name: {{ include "igris.fullname" . }}
  labels:
    {{- include "igris.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "igris.selectorLabels" . | nindent 4 }} 