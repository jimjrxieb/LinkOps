apiVersion: v1
kind: Service
metadata:
  name: {{ include "whis.fullname" . }}
  labels:
    {{- include "whis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "whis.selectorLabels" . | nindent 4 }} 