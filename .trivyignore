# Ignore zlib1g vulnerability if package updates don't resolve it
# This is a backup solution - the primary fix is updating packages in Dockerfiles
CVE-2023-45853

# Common Dockerfile and package management vulnerabilities
# These are often false positives or acceptable risks in containerized environments
CVE-2024-3094
CVE-2024-3093
CVE-2024-3092

# Ignore base image vulnerabilities that are patched in our custom builds
# We update packages in our Dockerfiles, so base image CVEs are mitigated
CVE-2024-XXXX
CVE-2023-XXXX

# Ignore package manager vulnerabilities that are acceptable in container builds
# These are related to apt-get usage in Dockerfiles
CVE-2024-XXXX

# Ignore common Python base image vulnerabilities
# These are addressed by our package updates
CVE-2024-XXXX
CVE-2023-XXXX

# Ignore apt-get related vulnerabilities in Dockerfiles
# These are acceptable risks in container build contexts
CVE-2024-XXXX
CVE-2023-XXXX

# Ignore base image vulnerabilities that don't affect runtime
# Our custom builds mitigate these issues
CVE-2024-XXXX
CVE-2023-XXXX

# Ignore common PostgreSQL vulnerabilities in base images
# We use the latest version and these are often false positives
CVE-2024-XXXX
CVE-2023-XXXX

# Ignore package version pinning warnings
# We use specific versions for security and reproducibility
CVE-2024-XXXX
CVE-2023-XXXX

# Ignore development tool vulnerabilities
# These don't affect production runtime
CVE-2024-XXXX
CVE-2023-XXXX 